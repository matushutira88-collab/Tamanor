import { PlaceholderScreen } from '@/components/shell/placeholder-screen';
import { t } from '@/i18n';

/** Accounts management — navigation hook for M4. No product functionality yet. */
export default function AccountsScreen() {
  return <PlaceholderScreen title={t.nav.accounts} />;
}
