import { PlaceholderScreen } from '@/components/shell/placeholder-screen';
import { t } from '@/i18n';

/** Alerts / action queue — navigation hook for M4. No product functionality yet. */
export default function AlertsScreen() {
  return <PlaceholderScreen title={t.nav.alerts} />;
}
