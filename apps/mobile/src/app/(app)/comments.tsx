import { PlaceholderScreen } from '@/components/shell/placeholder-screen';
import { t } from '@/i18n';

/** Comments/Inbox — navigation hook for M4. No product functionality yet. */
export default function CommentsScreen() {
  return <PlaceholderScreen title={t.nav.comments} />;
}
