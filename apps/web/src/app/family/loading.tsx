import { FamilyPageSkeleton } from "./skeletons";
// CS-C6.1 — /family segment loading boundary (also covers onboarding while it streams).
export default function Loading() { return <FamilyPageSkeleton />; }
